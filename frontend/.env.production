# Production build config (loaded by `next build` in production).
# Overrides the localhost value in .env for production builds.
# NOTE: Vercel/Render dashboard env vars take precedence over this file.
# The live production backend is classvortex.onrender.com (confirmed via
# /health). attendvortex-api.onrender.com no longer exists (404).
NEXT_PUBLIC_API_URL=https://classvortex.onrender.com/api/v1